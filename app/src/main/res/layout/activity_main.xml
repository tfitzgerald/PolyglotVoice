<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Initializing AI..."
        android:textColor="#00FF00"
        android:textStyle="bold" />

    <ScrollView
        android:id="@+id/scrollTranscript"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/controls"
        android:layout_below="@id/tvStatus"
        android:layout_marginVertical="10dp">
        <TextView
            android:id="@+id/tvTranscript"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="18sp" />
    </ScrollView>

    <LinearLayout
        android:id="@+id/controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:gravity="center"
        android:orientation="vertical">

        <View
            android:id="@+id/pulseIndicator"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:alpha="0"
            android:background="@android:drawable/presence_online"
            android:backgroundTint="#00BCD4" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="16dp">
            <Button android:id="@+id/btnSave" android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content" android:text="SAVE" android:textSize="10sp" android:layout_margin="2dp" android:backgroundTint="#444444" android:textColor="#FFFFFF"/>
            <ToggleButton android:id="@+id/toggleFlavor" android:layout_width="0dp" android:layout_weight="1.2" android:layout_height="wrap_content" android:textOff="FLAVOR OFF" android:textOn="FLAVOR ON" android:textSize="10sp" android:layout_margin="2dp" />
            <Button android:id="@+id/btnReset" android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content" android:text="RESET" android:textSize="10sp" android:layout_margin="2dp" android:backgroundTint="#444444" android:textColor="#FFFFFF"/>
            <Button android:id="@+id/btnClear" android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content" android:text="CLEAR" android:textSize="10sp" android:layout_margin="2dp" android:backgroundTint="#444444" android:textColor="#FFFFFF"/>
        </LinearLayout>

        <FrameLayout
            android:id="@+id/btnListenFrame"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:background="@android:drawable/screen_background_light_transparent"
            android:clickable="true"
            android:focusable="true">
            <ImageView
                android:id="@+id/btnListenIcon"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:layout_gravity="center"
                android:src="@android:drawable/ic_btn_speak_now"
                android:contentDescription="Listen"
                android:tint="#FFFFFF" />
        </FrameLayout>
    </LinearLayout>
</RelativeLayout>